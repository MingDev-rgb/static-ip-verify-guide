# 09 · 实测数据集（18 家 · 2026-09）

> **说明**：本表全部为**实测/官方页面的客观记录**。
> 每条包含：**价格 / 起订门槛 / 支付方式 / 退款政策 / 实测 ASN 结果**。
> **ASN 结果均可用文档 01 的方法复现**（RDAP + RIPEstat + Scamalytics）。
> **数据采集时间：2026 年 9 月** —— 价格与政策会变，请以各站当前页面为准。
>
> 我们**不对任何商家作主观评价**：下表只记录"查到了什么"。

---

## 一、总表

| # | 商家 | 层级 | 产品 / 国家 | 价格 | 起订 / 门槛 | 支付 | 退款政策 | **实测 ASN 结果** |
|---|---|---|---|---|---|---|---|---|
| 1 | **NovProxy** | 下游 | 德国静态 ISP | $3.99/30天；7 天 $1.9 | 1 个 | 支付宝 | 条款 1.2：**「质量不达标」可退余额**（免手续费） | IP 实测 → **AS395793 Arisk Communications inc.**（ariskisp.com，主营 VPS/独服/GPU 云）；Scamalytics `Server = Yes` |
| 2 | **LinkStatic** | 下游 | 静态 ISP | 德国原生 $3.60 / 广播 $2.40；**7 天 -50%**（新户 ≈$1） | 1 个 | 支付宝 / 加密货币 / 卡 | 「**非 IP 质量问题不予退款**」 | 德国 **客服原话：「只有这个号段」**（实测 `145.79.x` → RDAP netname = Arisk）；美国池实测混有 **I-2000, Inc.**（小 ISP，`Server = No`） |
| 3 | **LokiProxy** | 下游 | 德国静态住宅 | $6.99（30 天 7 折 $4.89） | 1 个 | 未核 | 未核 | 德国示例 IP `145.79.75.*` → **广播 AS2914 NTT America**；其页面把「**双 NTT 线路**」作为 $4.99 档卖点 |
| 4 | **1024proxy** | 下游 | 德国 | $2.0 | 1 个 | 支付宝 | **客服明确回复：机房段「不算」IP 问题** → 不退不换 | 样本 IP 实测 → **AS174 Cogent Communications** |
| 5 | **ZooProxy** | 下游 | 德国 | 原生 $2.0 / 广播 $1.8 | 1 个 | 支付宝 | 有条件（USDT $2 / 卡 5%） | 样本 IP 实测 → **DataZone / Sejong**（小网络公司） |
| 6 | **IPMAX** | 下游 | 德国静态住宅 | **$8 / IP** | 1 个 | 支付宝 | 明确「**静态住宅不退款**」 | 样本 IP 实测 → **Cogent**；其《反洗钱合规计划》为美国券商模板（BSA/FINRA/FinCEN） |
| 7 | **Proxy-Cheap** | 中游 | 静态住宅（可指定 ISP） | $2.44~3.59/月；**7 天试用 $1.99** | 1 个，但**最低充值 $9** | 卡（**实际结账页无支付宝**） | 「**仅永久关户时退余额**」；条款提及 KYC | 可指定 ISP（Liberty Global / Lumina / Lietpark）；**未实测** |
| 8 | **Bart Proxies** | 上游 | ISP 代理 | $2~3/IP | ⚠️ **25 个起（$50~75）** | 卡 / 加密货币 | 「**All sales are final**」 | 官网德国页写 Deutsche Telekom AS3320，但**购买配置器里只有美国**（AT&T/T-Mobile/RCN，Virginia 机房）；$5 试用 = **AT&T 1 小时 5 个代理** |
| 9 | **Quantum Proxies** | 上游 | DTAG ISP | $1.75/IP | ⚠️ **25 个起（$45/30天）** | 卡 | 未核 | 面板实测：**DTAG ISP $45 / 25 个 / 30 天**；德国 DC $80 / 50 个（缺货） |
| 10 | **RoundProxies** | 上游 | ISP 代理 | **$1.4/IP** | ⚠️ **10 个起（$14/月）** | 卡 / PayPal / 加密货币 | **ToS 6.5.1：24h 内重大缺陷可全额退** | 官网 ISP 目录列出 **Deutsche Telekom AS3320 / Vodafone Deutschland AS25135** |
| 11 | **proxying.io** | 上游 | AT&T / DT | $1.4~2.25/IP | 未核 | 卡 / PayPal / **支付宝** | **Strict No Refund**；邮件不回 | 页面写 "Residential IPs from Deutsche Telekom" |
| 12 | **Cliproxy** | ? | 静态 ISP | 首页写 $1.8~3.0/IP | ? | ? | ? | ⚠️ **`/pricing` 与 `/zh/pricing` 用 curl 与真浏览器均返回 403**（WAF 拦截）→ **无法核实** |
| 13 | **922S5（新站 922proxys5.com）** | 下游 | 按 IP 计费 | $0.05/IP | ? | **只收加密货币** | — | 明文 "no KYC / ISP targeting"；站点标注 "Reopening Sale" |
| 14 | **量子跃动** | 下游 | 德国 | ¥60/IP | 1 个 | 支付宝 / 微信 | 24h 内可免费更换 | 注册需国内手机号 |
| 15 | **辣椒HTTP** | 下游 | — | ¥34.8 | 1 个 | 支付宝 | — | **提取前必须完成身份验证** |
| 16 | **TTSOP** | 下游 | 德国双 ISP | ¥55/月 | 1 个 | 微信 | 3 天地区不符可退/换 | 官网要求「需自备海外网络环境」 |
| 17 | **IPdodo** | 下游 | 德国 | ¥78/月 | 1 个 | 支付宝 | **明确不支持退款** | — |
| 18 | 光梭IP / 鲸云网络(51TCP) / 6nn 等 | 下游 | — | 未核 | — | 应支持支付宝 | 未核 | 页面只有营销页/登录页，**无可核对信息** |

### 附：伪装成"评测"的推广渠道

| 站点 | 性质 |
|---|---|
| **jiyueip.com（极跃圈）** | ⚠️ **不是供应商，是"带返佣参数的推广导航站"** —— 它的"很夯 / 非常夯"是**排位**，链接里带 `inviteCode=` / `p=admin02` 等返佣标识；页面自称"专属优惠通道，仅限此通道注册的用户可享受优惠"。**它的"推荐"有立场，不是评测。** |

---

## 二、按"层级"重排

### 上游（敢写 ASN，但批量起订）

| 商家 | 价格 | 起订 | 是否写 ASN |
|---|---|---|---|
| Bart Proxies | $2~3/IP | **25 个** | ✅ 写 Deutsche Telekom AS3320（但德国不在购买配置器里） |
| Quantum Proxies | $1.75/IP | **25 个** | ✅ 写 DTAG |
| RoundProxies | $1.4/IP | **10 个** | ✅ 写 DT AS3320 / Vodafone AS25135 |
| proxying.io | $1.4~2.25/IP | 未核 | ✅ 写 Deutsche Telekom |
| Bright Data | $3.5/IP | **10 个** | ✅ 写 ISP |

### 中游（1 个起，但有门槛）

| 商家 | 价格 | 门槛 |
|---|---|---|
| Proxy-Cheap | $2.44~3.59/月 | **最低充值 $9** |
| LokiProxy ⭐ | $6.99（30 天 7 折 $4.89） | 1 个起（**不能选 ISP**，$6.99 只是更贵的下游） |

### 下游（1 个起 + 支付宝，随机段）

| 商家 | 价格 | 支付 |
|---|---|---|
| NovProxy | $3.99/30天；7天 $1.9 | 支付宝 |
| LinkStatic | 德国 $2.4~3.6；7 天 -50% | 支付宝 |
| 1024proxy | $2.0 | 支付宝 |
| ZooProxy | $1.8~2.0 | 支付宝 |
| IPMAX | $8 | 支付宝 |
| 量子跃动 | ¥60 | 支付宝/微信 |
| TTSOP | ¥55 | 微信 |
| IPdodo | ¥78 | 支付宝 |
| 辣椒HTTP | ¥34.8 | 支付宝 |

---

## 三、从这张表能读出的三个事实

### 事实 1：**"1 个 + 真运营商 ASN + ≤$5"这个组合不存在**

- 写 ASN 的（上游）：**10~25 个起**，$14~$75
- 1 个起卖的（下游）：**不写 ASN**，且实测样本多为机房段
- **两者没有交集**（我们实测 18 家）

### 事实 2：**多家下游的德国货来自同一批号段**

| 商家 | 宣称 | 实测 |
|---|---|---|
| NovProxy | 德国原生住宅 | AS395793 **Arisk Communications inc.** |
| LokiProxy | 德国原生 ISP（"双 NTT"） | `145.79.75.*` → 广播 **AS2914 NTT** |
| LinkStatic | 德国原生 ISP | `145.79.126.0/24` → RDAP netname = **Arisk**，广播 AS2914 NTT |

→ **三家指向同一个号段持有者**（复现方法见文档 01、案例见文档 03）

### 事实 3：**"原生""双ISP""双NTT"这类词，与"是不是住宅"无关**

- "原生" = 注册国与位置国一致（本地机房段也是"原生"）
- "双 ISP" = 该段历史上挂过多个运营商名字（倒手痕迹）
- "双 NTT" = 由 Tier-1 骨干广播 —— **骨干网络正是最像机房的一类**

---

## 四、如何自己复现这张表里的每一个 ASN 结果

```bash
# ① 号段持有者（RIR 官方注册数据）
curl -s "https://rdap.db.ripe.net/ip/<IP>" | jq '{handle, name, country, type}'

# ② 当前广播方（BGP）
curl -s "https://stat.ripe.net/data/prefix-overview/data.json?resource=<IP>" \
  | jq '.data | {resource, asns: [.asns[] | {asn, holder}]}'

# ③ 第三方风控判定（需浏览器，有反爬）
# https://scamalytics.com/ip/<IP>   → 看 Proxies → Server 字段
```

**如果某个商家的示例 IP 用上面三步查出来的 `netname` 是主机商、且 `Server = Yes`，那它就是机房段。** —— 这不是我们的判断，是 RIR 注册记录与风控库字段的显示结果。


---

# 附录 A · "下游 = 抽卡"：机制、证据与池子地图

> 这一节是整份调研里**最有冲击力的发现**，所以放在最前面看。
> 全部结论都有可复现证据（控制台原文、官方 geofeed、RDAP）。

## A1. 提货机制本身就是"抽卡"

下游零售商的提取面板通常提供两种方式：

| 方式 | 实际行为 |
|---|---|
| **「快速提取」** | **按条件（国家/城市/有效期）随机分配一个 IP** ← 这就是抽卡 |
| 「批量提取」 | 先在列表里勾选具体 IP（可以避开某些号段）—— 但前提是列表里**有**别的号段 |
| 「单条提取」 | 列表每行右侧的按钮，指定提取 |

**关键**：能不能抽到好东西，**不取决于运气，取决于它的库存里有什么**。而库存来源是固定的（见 A2）。

## A2. 为什么抽不到好东西：库存来源被锁死

实测发现：**下游某家的德国库存，与某个 IP 经纪人的官方号段精确吻合**。

| 面板里看到的打码形态 | 该经纪人官方 geofeed 中的号段 | 吻合 |
|---|---|---|
| `82.*.243.x` | `82.198.243.0/24`（DE, Frankfurt am Main） | ✅ 第三段一致 |
| `145.*.142.x` | `145.79.142.0/24`（DE, Frankfurt am Main） | ✅ 第三段一致 |
| `46.*.201.x` | 同族德国段 | ✅ |

**该经纪人（Arisk Communications Inc.）持有的德国段共 96 条**（首段分布：`82` 系 44 条、`145` 系 79 条、`46` 系 12 条）。

→ **结论：它的德国库存很可能就是这一批。你抽来抽去，抽的都是同一批机房段。**

**这也决定了客服的答案**：当你要求"换一个真运营商段（Deutsche Telekom AS3320 等）"时，它**大概率只能回"没有该运营商资源"** —— 因为它的池子里就没有。

## A3. 池子地图：四个池子（哪几家共用同一个）

### 池 A · 同一个 IP 经纪人（Arisk Communications Inc.）

| 商家 | 实测落点 |
|---|---|
| **NovProxy** | `82.198.243.x` |
| **LinkStatic** | `145.79.126.x` |
| **LokiProxy** | `145.79.75.x` |
| **YumiProxy** | `145.79.75.x`（与 LokiProxy 展示的**同一个**德国示例） |

→ **四家的德国货来自同一个号段持有者。**
→ 佐证：NovProxy 与 LinkStatic 的德国 IP，在 ping0 的「**企业**」栏**都写着 Arisk** —— 区别只是**广播方**不同。

### 池 B · Tier-1 骨干段（机房属性最强）

| 商家 | 实测落点 |
|---|---|
| **1024proxy / IPMAX** | **AS174 Cogent Communications** |
| **LokiProxy** | **AS3257 GTT** / **AS2914 NTT America** |

### 池 C · 小网络公司段

| 商家 | 实测落点 |
|---|---|
| **1024proxy / YumiProxy / ZooProxy** | **AS7361 DataZone Networks**（马来西亚）/ **AS9848 SEJONG NETWORKS**（韩国） |

### 独立一档 · 干净的小 ISP（只在某家的**美国池**里出现）

| 商家 | 实测落点 |
|---|---|
| **LinkStatic（美国池）** | **I-2000, Inc.**（`207.97.128.0/18`，ARIN **DIRECT ALLOCATION**）→ `Server = No`、ping0 风控 **3%** |

> ⭐ **一个关键细节**：**"能选 ISP 的下拉"只在该商家的美国档出现** ——
> 德国档问客服，直接得到「**只有这个号段**」。
> 这解释了为什么"德国池抽不到好东西"：**它的货架上就只有那一个号段。**

### 对照：上游阵营敢写具体 ASN

| 商家 | 页面写的是 |
|---|---|
| RoundProxies | **Deutsche Telekom AS3320 / Vodafone Deutschland AS25135** |
| Quantum | **DTAG ISP** |
| Bart Proxies | **Deutsche Telekom AS3320** |
| proxying.io | "Residential IPs from **Deutsche Telekom**" |

> ⭐ **RoundProxies 官网上专门有一段「Not All 'ISP' Proxies Are Really ISPs」** ——
> 上游自己在教育市场："很多商家把机房 IP 标成 ISP 代理"。

## A4. 抽卡结果的两极：美国池 vs 德国池

同一个商家（LinkStatic），两个国家的池子质量完全不同：

| 池子 | 实测结果 |
|---|---|
| **德国池** | 客服原话「**只有这个号段**」→ 实测 = Arisk 机房段（Scamalytics `Server = Yes`） |
| **美国池** | 混有 **I-2000, Inc.**（ARIN **DIRECT ALLOCATION** 的小 ISP，`207.97.128.0/19`）：**`Server = No`**、Fraud Score 0、ping0 风控值 **3%（极度纯净）** |

**实测拿到的那一个**（$0.90 / 7 天）：
- Scamalytics `Proxies → Server` = **No**（不是商用服务器）
- ping0 风控值 **3%**（极度纯净档）
- 持有者 = **ARIN 直接分配的小 ISP**（不是主机商）

→ **结论：只有"美国号段"的池子里能抽到干净的小 ISP 段；德国池就是同一批机房段。**

## A5. 价格倒挂：贵的反而货差

| 阵营 | 价格 | 拿到的货 |
|---|---|---|
| **下游零售**（中文界面 + 支付宝） | **$4.8 ~ $10.8 / IP / 月** | 机房段（Arisk）或小 ISP 段（DataZone / Sejong） |
| **上游 / 近上游**（敢写 ASN） | **$1.00 ~ $1.75 / IP / 月** | **Deutsche Telekom AS3320** 这类真运营商段 |

> **"一分钱一分货"在这个行业不成立 —— 中间商才是最贵的。**
> 下游拿不到大运营商段（那类资源被少数上游垄断、要签合同），
> 只能买批发市场上**最便宜的那批段**，然后统一贴"原生住宅"标签。

**缺的从来不是钱（$5 在上游能买到 DT AS3320），缺的是能直连上游的支付方式。**

## A6. 判据补充（从实测中总结的新信号）

| 信号 | 含义 |
|---|---|
| 控制台样本表的 **ASN 列写着 "ISP ISP"** | ⚠️ 下游标准做法 —— **上游一定敢写 `AS3320`** |
| **IP 没有 PTR 反解** | ⚠️ 真家宽 IP 几乎都有 rDNS；无 PTR 常见于批发/机房段 |
| 测试额度给的是"动态住宅流量"或"短效 ISP" | ⚠️ **和要买的静态 ISP 不是一个池子、不是同一批 IP** → 测了也不能外推 |
| 页面写"**严格 KYC（全客户旅程）**" | ⚠️ 与 IPRoyal 措辞一字不差 → 属于同一套合规模板 |
| **"IP 独占权跟租期走"** | ⚠️ 到期不续 → 该 IP **重新进入池子**（会被分给别人）；续费时那个 IP 还在不在池子里，是长期养号的关键 |
| **7 天档 vs 30/90 天档** | 7 天 = 验货用；**要长期养固定身份，直接上 30/90 天**（一旦到期重提拿到另一个 IP，"IP 历史一致性"就断了） |

## A7. 两个反直觉洞察

### ① "小 ISP 段" 好过 "Tier-1 骨干段"

| 类型 | 例子 | 风控库判定 |
|---|---|---|
| **Tier-1 骨干** | NTT AS2914 / GTT AS3257 / Cogent AS174 | **Server = Yes / 数据中心** ❌ |
| **小 ISP** | I-2000（ARIN 直接分配的小 ISP） | **Server = No / 家庭宽带** ✅ |

→ 所以**把"双 NTT"当卖点是反的**：**NTT 恰恰是最像机房的那一类**。

### ② Scamalytics 的滑块是"陷阱"（以及 ipinfo 会漏判）

| 事实 | 说明 |
|---|---|
| 滑块只调 **Fraud Score** | 而**机房段也可以是 0 分** —— 实例：Arisk 段 `Fraud Score = 0` 但 `Server = Yes` |
| → 拖到最严（0）也**筛不掉机房段** | 真判据是 **`Server` / `Residential Proxy`** 字段 + **RDAP netname** |
| ⚠️ **ipinfo 会漏判** | 对 `145.79.126.x` 这类段，**ipinfo 的 `org` 字段直接返回空** → 只靠它看会走眼 |


---

# 附录 B · `Server` 判据的完整矩阵（2026-09 补测）

> 这一节是调研后期补做的**同口径对照测试**，它**修正了本文前面一个容易被误读的印象**：
> **"小 ISP 段比大运营商段更好" —— 这个说法不成立。**

## B1. 实测矩阵（同口径：RDAP + Scamalytics）

| 段类型 | 实例 IP | RDAP `registrant` | 分配类型 | **Scamalytics `Server`** |
|---|---|---|---|---|
| **大运营商（消费级）** | `32.126.202.214` | **AT&T Enterprises, LLC** | DIRECT ALLOCATION | ✅ **No** |
| **小 ISP** | `68.235.180.70` | **Vianet Inc.** | DIRECT ALLOCATION | ✅ **No** |
| **小 ISP** | `207.97.137.x` | **I-2000, Inc.** | DIRECT ALLOCATION | ✅ **No** |
| **Tier-1 骨干** | `207.152.64.x` | **NTT America, Inc.** | DIRECT ALLOCATION | ❌ **Yes** |
| **Tier-1 骨干** | `192.217.228.48` | **NTT America, Inc.** | DIRECT ALLOCATION | ❌ **Yes** |
| **Tier-1 骨干** | `204.1.124.2` | **NTT America, Inc.** | DIRECT ALLOCATION | ❌ **Yes** |
| **经纪人（借骨干广播）** | `64.50.130.19` | **Arisk**（RDAP 显示 GTT-CUSTOMER） | ASSIGNMENT | ❌ **Yes** |

## B2. 结论：分界线不在"大小"，在"是不是运营商"

```
✅ registrant 是「运营商 / ISP」   →  Server = No
   （无论大小：AT&T、Vianet、I-2000 全部通过）

❌ registrant 是「骨干网 / 主机商 / 经纪人」  →  Server = Yes
   （NTT America、GTT、Cogent、Arisk 全部不通过）
```

**⚠️ 这条修正很重要**，因为很多攻略会写"要找小 ISP 段、小 ISP 更干净" ——
**实测结果是：大运营商（AT&T）同样判 `Server = No`。**

**小 ISP 的"优势"其实只有两条**（都不是"判据"层面的）：

| 小 ISP 的真实优势 | 说明 |
|---|---|
| **共享人数少** | 段小众 → 用的人少 → 更接近"独享" |
| **被滥用程度低** | 段冷门 → 黑名单命中率低 |

**但它也有明显缺点**：
- **库存极小**（一个 `/18` 才 16384 个 IP），**说没就没**（见 B3）
- **供给不稳定**，商家随时可能拿不到货

**→ 所以选段的正确顺序是**：
```
① 先看 registrant 是不是运营商（大小不限）   ← 硬判据
② 再看 Server 是不是 No                    ← 硬判据
③ 最后才考虑"冷门程度 / 共享人数"           ← 软偏好
```

## B3. 抽卡池会枯竭（同一条产品线的前后对比）

**用同一家下游（美国档）在不同时间点的实测样本做对照**：

| 时间 | 拿到的段 | Scamalytics `Server` |
|---|---|---|
| **早期** | `207.97.128.0/18`（**I-2000, Inc.**，小 ISP） | ✅ **No**（风控值 3%，极度纯净） |
| **后期（同一条产品线）** | `204.1.124.2` / `207.152.64.200` / `207.152.64.198` / `192.217.228.48`（**NTT America**） | ❌ 全部 **Yes** |
| **后期** | `64.50.130.19`（**Arisk**，经 GTT 广播） | ❌ **Yes** |

**→ 结论：**

> **下游的"好段"是有寿命的** ——
> 一个池子的好货被抽完，之后**同一条产品线**给你的，**只是同一批差段换了个 IP**。
>
> **验证方法**：**隔一段时间再要一次示例 IP，看段有没有变。**
> · 段变了、且仍是运营商段 → 商家补货了 ✅
> · 段没变 or 全变成骨干段 → **它的池子就那点货** ❌

## B4. "挂个 dsl 域名"骗不过风控

`64.50.130.19` 的一个细节：

```
hostname         = 64-50-130-19.client.dsl.net    ← 故意挂着 dsl.net 域名
Connection type  = dsl                            ← 自己标成"数字用户线"
但 Scamalytics   = Server: Yes + "not a standard domestic connection,
                    it is a commercial server"    ← 风控库不认
```

**→ 结论**：**`hostname` 和 `Connection type` 是自己填的，可以造假；`Server` 字段是第三方判的。**
**看后者，别看前者。**

## B5. 一个方法论修正：**`ISP Name` 与 `Organization Name` 两个字段都要看**

之前本文写的是"**`Organization Name` 才是真相**"（德国案例里：`ISP Name = NTT`、`Organization = Arisk`）。

**但补测时出现了相反的情况**：

| IP | `ISP Name` | `Organization Name` | 真正持有者 |
|---|---|---|---|
| `64.50.130.19` | **Arisk Communications inc** ← **这里 ISP 栏才是真相** | GTT | **Arisk** |

**→ 修正后的读法**：
> **两个字段都读，哪个是"运营商/经纪人"就信哪个，另一个是"广播方/管道"。**
> **不要把"Organization Name"当成唯一真相** —— 字段位置在不同记录里会换。

## B6. 复现命令

```bash
# ① 号段持有者（谁的名下）
curl -s -H "Accept: application/rdap+json" "https://rdap.arin.net/registry/ip/<IP>" \
  | jq '{name, handle, startAddress, endAddress, type}'

# ② 广播方（谁在广播）
curl -s "https://stat.ripe.net/data/prefix-overview/data.json?resource=<IP>" \
  | jq '.data | {resource, asns:[.asns[]|{asn,holder}]}'

# ③ 风控字段（浏览器打开，反爬）
#   https://scamalytics.com/ip/<IP>
#   → 看 Proxies → Server，以及 ISP Name / Organization Name 两栏
```
